## To run the project
- Create a virtual environment in the root project folder.
- Go into the virtual environment and install packages from requirements.txt using below command:
```sh
pip install -r requirements.txt
```
- All the packages will be installed in the virtual environment and can run each and every cell in .ipynb file.